from setuptools import setup

setup(
    name="ky_bypass",
    version="1.0",
    description="",
    author="",
    author_email="",
    packages=["kf_bypass"],
    install_requires=[
        "requests",
    ],
)
